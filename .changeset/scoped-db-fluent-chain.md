---
"@porulle/core": patch
---

A join-then-where chain could replace the pre-applied organization predicate with the caller's condition.
A scoped handle now keeps its organization predicate through every fluent method.
